module
/-- The problem-file version: same statement as `ModMerge.Good.shared`, proof is a
`sorry`. In this module's base `.olean` it is an axiom; the sorried proof is only in
`.olean.private`. -/
public theorem shared : True := by sorry
